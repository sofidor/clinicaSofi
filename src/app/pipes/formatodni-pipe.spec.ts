import { FormatodniPipe } from './formatodni-pipe';

describe('FormatodniPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatodniPipe();
    expect(pipe).toBeTruthy();
  });
});
