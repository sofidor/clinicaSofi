import { DisableIfEmpty } from './disable-if-empty';

describe('DisableIfEmpty', () => {
  it('should create an instance', () => {
    const directive = new DisableIfEmpty();
    expect(directive).toBeTruthy();
  });
});
