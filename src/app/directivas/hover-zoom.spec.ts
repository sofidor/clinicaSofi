import { HoverZoom } from './hover-zoom';

describe('HoverZoom', () => {
  it('should create an instance', () => {
    const directive = new HoverZoom();
    expect(directive).toBeTruthy();
  });
});
