import { AppBackdropClick } from './app-backdrop-click';

describe('AppBackdropClick', () => {
  it('should create an instance', () => {
    const directive = new AppBackdropClick();
    expect(directive).toBeTruthy();
  });
});
