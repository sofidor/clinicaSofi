import { MostrarPassword } from './mostrar-password';

describe('MostrarPassword', () => {
  it('should create an instance', () => {
    const directive = new MostrarPassword();
    expect(directive).toBeTruthy();
  });
});
